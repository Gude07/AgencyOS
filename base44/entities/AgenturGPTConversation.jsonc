{
  "name": "AgenturGPTConversation",
  "type": "object",
  "properties": {
    "agency_id": {
      "type": "string",
      "description": "ID der Agentur"
    },
    "title": {
      "type": "string",
      "description": "Titel des Gespr\u00e4chs"
    },
    "messages": {
      "type": "string",
      "description": "JSON-String der Nachrichten"
    },
    "folder_id": {
      "type": "string",
      "description": "Optionale Ordner-ID (lokal)"
    },
    "mode": {
      "type": "string",
      "enum": [
        "normal",
        "deep_research"
      ],
      "default": "normal",
      "description": "Recherche-Modus"
    }
  },
  "required": [
    "agency_id",
    "title"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "data.agency_id": "{{user.data.agency_id}}"
    },
    "update": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}