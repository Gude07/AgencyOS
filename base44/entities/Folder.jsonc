{
  "name": "Folder",
  "type": "object",
  "properties": {
    "agency_id": {
      "type": "string",
      "description": "ID der Agentur"
    },
    "name": {
      "type": "string",
      "description": "Name des Ordners"
    },
    "description": {
      "type": "string",
      "description": "Beschreibung des Ordners"
    },
    "color": {
      "type": "string",
      "enum": [
        "blue",
        "green",
        "purple",
        "orange",
        "red",
        "gray"
      ],
      "default": "blue",
      "description": "Farbe des Ordners"
    }
  },
  "required": [
    "agency_id",
    "name"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "data.agency_id": "{{user.data.agency_id}}"
    },
    "update": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.agency_id": "{{user.data.agency_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}